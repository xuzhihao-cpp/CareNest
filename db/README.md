# db

数据库目录，存放建表 SQL、迁移脚本、初始化数据、统计 SQL 和 ER 图。

## 子目录

| 目录 | 用途 |
| --- | --- |
| `schema/` | 建表 SQL，阶段 3 开始维护基础表结构。 |
| `seed/` | 演示账号、演示数据和初始化数据。 |
| `migration/` | 后续字段变更、表结构升级和数据修复脚本。 |

阶段 1-2 不创建业务表。阶段 3 开始写入基础表结构，所有共享字段必须先进入 `docs/dictionary/data-dictionary.md`。