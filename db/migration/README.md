# db/migration

存放后续字段变更、表结构升级和数据修复脚本。

规则：

- 迁移脚本按时间或阶段顺序命名。
- 已完成并确认的字段最终要合入 `db/schema/` 基线，确保新环境不需要重放历史迁移。
- `db/migration/` 只用于升级已有数据库，必须逐个确认前置结构，禁止在空库初始化时批量盲目重放。
- 任何字段变更必须同步更新 `docs/dictionary/data-dictionary.md`。
- 影响接口字段时必须同步更新 `docs/api/` 和 `mock/`。
