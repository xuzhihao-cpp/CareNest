# Phase 19-A Docker acceptance

日期：2026-07-12

## 配置检查

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 基础 Compose 与应用 Compose 可合并解析 | 通过 | `docker compose ... config` 返回 0 |
| 用户后端宿主机构建 | 通过 | Maven package 返回 0 |
| 护理/管理后端宿主机构建 | 通过 | Maven package 返回 0 |
| 前端 H5 生产构建 | 通过 | `pnpm build:h5` 返回 0 |
| Docker 镜像构建 | 通过 | 双后端与前端三份镜像均完成干净构建 |
| 全部容器健康 | 通过 | MySQL、Redis、MinIO、双后端、前端共 6 个服务均为 healthy |
| Nginx 真实路由与登录 | 通过 | 四角色登录及用户/护理/管理接口均经 `http://localhost:3000/api/v1` 返回真实数据 |
| 后端独立重建 | 通过 | 强制重建双后端后前端容器 ID 不变，Docker DNS 重新解析成功 |
| 权限错误状态 | 通过 | 用户端与护理/管理端缺少登录凭证均返回 HTTP 401，不再返回 500 |
| 数据库真实记录 | 通过 | `users=5`、`services=2`、`orders=1`、`reports=1`、`bindings=1` |
| 首次空库初始化 | 通过 | 独立空卷执行 17 个 schema/seed 文件，关键表与阶段 6 待确认字段均存在 |
| Redis 与 MinIO | 通过 | Redis 返回 `PONG`；密码模式下未认证返回 `NOAUTH`、认证后返回 `PONG`；MinIO 初始化任务成功创建业务桶 |
| 非 root 与只读运行 | 通过 | 双后端 UID 10001、前端 UID 101；双后端根文件系统只读 |

## 成员检查范围

- 成员 1：确认首次空卷执行全部 schema 和 seed，已有卷不被自动删除。
- 成员 2：确认 8081 健康、登录、绑定和档案接口通过 Nginx 工作。
- 成员 3：确认 8082 健康、订单、派单、护理任务和报告接口通过 Nginx 工作。
- 成员 4：确认前端生产资源、四端路由、刷新回退和错误状态正常。
- 组长：确认统一入口完成跨端链路，且没有运行时 mock 或直连错误端口。

## 实际联调结果

- 长辈：首页摘要返回成功。
- 家属：绑定列表 1 条、订单列表 1 条。
- 护理：工作台返回成功、任务列表 1 条。
- 管理：概览返回成功、订单列表 1 条。
- 前端首页、Nginx 健康页、用户后端健康页、护理/管理后端健康页均可访问。
- 四端均通过容器入口完成实际登录和首屏渲染，页面宽度未超过视口，浏览器控制台无页面错误。
- 管理概览已移除写死的 `12 / 28 / 9`，当前显示真实接口返回的 `0 / 0 / 0`，与数据库状态一致。
- 本机默认端口存在占用，验收时仅将宿主机 MySQL、Redis、MinIO 临时映射为 13306、16379、29000/29001；容器内部端口与提交配置未改变。

## 遗留验证项

- `backend-care-admin` 完整测试通过。`backend-user` 测试库字段已与 MySQL 基线对齐，但当前分支完整套件仍有 6 个既有业务契约/夹具失败，包括 OpenAPI 快照漂移及共享 H2 数据状态冲突；它们不影响本次 Docker 镜像构建与真实 MySQL 联调，但应由对应接口负责人单独收敛。
