# 阶段 49-55 全栈整合验收记录

## 验收范围

- 阶段 49：培训文章完整读模型、创建、编辑、发布与下线。
- 阶段 50：按真实订单推荐文章、必读门禁和阅读记录。
- 阶段 51：管理端随访、提醒事务联动、家属授权读取。
- 阶段 52-53：基础运营与质量数据看板、Redis 短缓存和 MySQL 回源。
- 阶段 54：一键恢复标准演示基线及重复执行。
- 阶段 55：健康就绪字段、容器运行和最终联调。

验收日期：2026-07-17。所有接口均通过 `http://127.0.0.1:3000/api/v1` 的 Nginx 入口调用，没有直接使用后端端口作为验收替代。

## 本次补全

1. 培训文章列表和写操作统一返回标题、摘要、内容地址、标签、适用服务、风险标签、必读要求和状态。
2. 护理文章推荐返回可直接展示的中文文章信息，并按订单服务、长辈风险和护理任务归属读取。
3. 必读文章在前端打开后才允许确认；后端阅读结果写入 `nurse_article_reading`。
4. 新增家属随访读取路由，校验 `FAMILY`、`ACTIVE` 绑定和 `HEALTH_VIEW` scope。
5. 基础看板、质量看板和培训推荐接入 Redis；权限校验在缓存读取之前，Redis 不可用时回源 MySQL。
6. 健康接口增加明确的 `ready` 字段，并同步 OpenAPI 契约快照。
7. 修复演示重置的外键顺序：重建 `report_001` 前先清理其评价和投诉依赖，全部种子可重复执行。
8. 培训文章使用随 H5 构建发布的站内真实内容页 `/static/training/fall-prevention.html`，不依赖 mock、登录页回退或空地址。

## 自动化结果

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| 用户后端 | `mvn -pl backend-care-admin,backend-user -am test` | 100 项通过 |
| 护理/管理后端 | 同上 | 81 项通过 |
| 前端类型 | `pnpm typecheck` | 通过 |
| 阶段 49-55 前端 | `pnpm test:stage49-55` | 19 项通过 |
| 前端生产构建 | Docker `frontend` 多阶段构建 | 通过 |
| SQL 基线 | 按文件名顺序执行 `db/seed/*.sql` | 8 个文件全部通过 |

## 真实接口与角色

| 场景 | 角色 | 结果 |
| --- | --- | --- |
| 五个演示账号登录 | 长辈、家属、护理、管理员、客服 | 全部成功 |
| 培训文章完整列表 | 管理员 | 返回完整文章字段 |
| 订单文章推荐与阅读 | `nurse_demo` + `order_001` | 推荐 1 篇，阅读状态写入成功 |
| 文章正文 | 公共站内地址 | HTTP 200 |
| 基础与质量看板 | 管理员 | 返回真实卡片、趋势、完成率和质量分布 |
| 随访和提醒 | 管理员 | 同一事务创建随访与提醒任务 |
| 随访读取 | `family_demo` + `elder_001` | 读取成功 |
| 随访越权 | 长辈角色、未绑定长辈 | 均返回 HTTP 403 |
| 健康检查 | 公共 | `status=UP`、`ready=true`、`dbConnected=true` |

## MySQL、Redis 与 Docker

- MySQL 验证到文章、阅读记录、随访、提醒和操作日志均为真实记录。
- 随访创建后，`follow_up_record.created_reminder_task_id` 与 `reminder_task.task_id` 一致，提醒状态为 `PENDING`。
- Redis 生成以下三类键：
  - `carenest:training:recommendation:*:v1`
  - `carenest:dashboard:basic:*:v1`
  - `carenest:dashboard:quality:*:v1`
- 停止 Redis 后，推荐和两类看板仍返回成功并从 MySQL 读取；Redis 重启后恢复 `healthy`。
- 一键恢复演示基线连续执行两次均返回 `ready=true`、`scenarioCount=8`，恢复后相关缓存键清空。
- MySQL、Redis、MinIO、两个后端和前端容器最终均为 `healthy`。

## 页面检查

真实浏览器验证了以下页面：

- 管理端：培训文章、随访管理、数据看板。
- 护理端：学习资料，显示真实文章标题、摘要、必读和阅读状态。
- 家属端：随访记录，能读取标准演示随访。

页面中文内容正常，未发现业务接口错误。控制台仅有 uni-app 统计服务在本地环境无法连接的超时警告，不影响业务接口、数据写入或页面读取。

## 结论

阶段 49-55 的前端、后端、MySQL、Redis、Docker 和角色权限链路均已接通。运行页面不使用 mock，写操作有真实落库和操作日志，缓存故障不会伪造成功，演示基线可重复恢复。
