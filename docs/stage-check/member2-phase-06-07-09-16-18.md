# 成员 2 阶段 6/7/9/16/18 开发验收记录

分支：`feature/user-phase-06-07-09-16-18`

## 已完成

| 阶段 | 结果 | 证据 |
| --- | --- | --- |
| 6 绑定授权 | 已实现 | `POST /api/v1/family/bindings`、`GET /api/v1/family/bindings`、`POST /api/v1/elder/bindings/{bindingId}/approve`、`PUT /api/v1/family/bindings/{bindingId}/scopes`、`POST /api/v1/family/bindings/{bindingId}/revoke` |
| 7 长辈基础档案 | 已实现 | `GET /api/v1/elders/{elderId}/profile`、`PUT /api/v1/elders/{elderId}/profile`、`GET /api/v1/family/elders` |
| 9 服务地址 | 已实现 | `GET/POST /api/v1/elders/{elderId}/service-addresses`、`PUT/DELETE /api/v1/service-addresses/{addressId}` |
| 18 健康与演示数据状态 | 已实现 | `GET /api/v1/health`、`GET /api/v1/admin/demo-data/status` |

## 阶段 16 状态

成员 2 PDF 要求阶段 16 使用 `care_report_ack`、`health_info_review_task`。

当前 `db/schema` 未提供这两张表，且用户要求“按照数据库定义的数据模型”，因此本次没有自建模型或伪实现阶段 16 接口。后续需要先补齐数据库 schema，再实现以下接口：

| Method | Path |
| --- | --- |
| POST | `/api/v1/elder/reports/{reportId}/ack` |
| POST | `/api/v1/family/reports/{reportId}/ack` |
| POST | `/api/v1/family/reports/{reportId}/archive-suggestions/decision` |

已按 PDF 固定 DTO 补充阶段 16 契约 mock，供前端在后端表结构未到位前联调字段：

- `mock/phase-16/elder-report-ack.json`
- `mock/phase-16/family-report-ack.json`
- `mock/phase-16/family-archive-suggestions-decision.json`

## 测试

已增加 `UserSidePhaseApiTest` 覆盖阶段 6/7/9/18 核心链路：

- 家属发起绑定、长辈确认、家属调整授权、家属撤销。
- 家属读取和修改已授权长辈档案。
- 家属维护服务地址和默认地址。
- 护士访问家属绑定接口被拒绝。
- 家属不能通过长辈确认接口代替长辈确认绑定。
- 健康检查返回阶段 1 契约字段。
- 管理员读取演示数据状态，护士被拒绝。

执行命令：

```powershell
& .\.tools\apache-maven-3.9.9\bin\mvn.cmd "-Dmaven.repo.local=.m2/repository" -pl backend-user "-Dtest=UserSidePhaseApiTest" test
```

结果：`Tests run: 7, Failures: 0, Errors: 0, Skipped: 0`。
