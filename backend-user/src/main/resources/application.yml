server:
  port: ${BACKEND_USER_PORT:8081}

spring:
  application:
    name: carenest-backend-user
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      connect-timeout: 2s
      timeout: 2s
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:smart_nursing}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USER:smart_nursing}
    password: ${MYSQL_PASSWORD:smart_nursing123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 21MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

carenest:
  app:
    version: ${APP_VERSION:0.1.0}
  jwt:
    secret: ${JWT_SECRET:smart-nursing-local-secret}
    expire-hours: ${JWT_EXPIRE_HOURS:24}
  minio:
    endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
    access-key: ${MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${MINIO_SECRET_KEY:minioadmin}
    bucket: ${MINIO_BUCKET:smart-nursing}
    region: ${MINIO_REGION:us-east-1}
    public-endpoint: ${MINIO_PUBLIC_ENDPOINT:http://localhost:9000}
  ai:
    provider: ${AI_PROVIDER:rule}
    endpoint: ${AI_ENDPOINT:https://dashscope.aliyuncs.com/compatible-mode/v1}
    api-key: ${DASHSCOPE_API_KEY:}
    model: ${AI_MODEL:qwen-plus}
    timeout: ${AI_TIMEOUT:15s}
    asr-endpoint: ${AI_ASR_ENDPOINT:${AI_ENDPOINT:https://dashscope.aliyuncs.com/compatible-mode/v1}}
    asr-api-key: ${AI_ASR_API_KEY:${DASHSCOPE_API_KEY:}}
    asr-model: ${AI_ASR_MODEL:qwen3-asr-flash}
    asr-timeout: ${AI_ASR_TIMEOUT:20s}
